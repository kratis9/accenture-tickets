/**
 * Application config, for the real app, we can have multiple configs based on the environment
 */

export default class Config {
  static username = "candidate_test1"
  static password = "candidate_test1"
  static recordsPerPage = 50
  static recordsPerAPICall = 500
}
